test:
    script:
        - export PATH=/nas/vplworker/anaconda3/bin:$PATH
        - source activate ivt
        - virtualenv .
        - bin/pip install --upgrade -r requirements.txt
        - python3 --version
        - pwd
        - rm -rf output
        - mkdir output
        - bin/python3 -u run.py config_gitlab.yml
        - rm -rf output
